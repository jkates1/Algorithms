public struct ShellSort  {
    /*
    public func sort<A: Equatable>(a: [A]) {
        var n = a.count
        var h = 1
        while (h < n/3) {
            h = 3*h + 1}
        while (h >= 1) {
            for (int i = h; i < n; i++) {
                for (int j = i; j>= h && less)
            }
            
            
        }
    }
 */   
}
